import ready from "./ready";
import pinned from "./pinned_message";
import completed from "./update_completed";
import slashCommand from "./slash_command";

export default [
	ready,
	pinned,
	completed,
	slashCommand,
];
